import { PlanHomeClient } from "@/components/plan/plan-home-client";

export const metadata = {
  title: "Manifest",
  description:
    "A quieter Manifest home — vision board, values, reflection questions, and the life areas you’re tending.",
};

export default function IdeateMyPage() {
  return <PlanHomeClient />;
}
