"use client";

import { SpaRedirect } from "@/components/spa-redirect";

/** Focus authenticated UI lives on the Vite SPA. */
export default function FocusAppRedirectPage() {
  return <SpaRedirect />;
}
