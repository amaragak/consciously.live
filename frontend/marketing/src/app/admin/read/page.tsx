export const metadata = {
  title: "Admin — Read",
};

/** Soft-redirects to the SPA admin CMS via parent SpaRedirect layout. */
export default function AdminReadPage() {
  return (
    <p className="mx-auto max-w-md px-4 py-20 text-sm text-muted">
      Opening Read CMS…
    </p>
  );
}
