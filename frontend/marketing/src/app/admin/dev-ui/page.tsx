import { AdminDevUiPanel } from "@/components/admin-dev-ui-panel";

export const metadata = {
  title: "Admin — Dev UI",
};

export default function AdminDevUiPage() {
  return <AdminDevUiPanel />;
}
