import { AdminLibraryCategoriesPanel } from "@/components/admin-library-categories-panel";

export const metadata = {
  title: "Admin — Category images",
};

export default function AdminCategoryImagesPage() {
  return <AdminLibraryCategoriesPanel />;
}
