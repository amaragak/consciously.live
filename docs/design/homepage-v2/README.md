# Consciously homepage v2

1. Copy `docs/design/homepage-v2/` into the root of the repo.
2. Open Cursor in Agent mode and paste everything in `CURSOR_PROMPT.md` below the `---` line.
3. Cursor investigates first and replies with a plan. Approve it before it edits anything.

Design references (design-tool files; treat them as specs, not code to paste):
- `Main.dc.html`: the desktop homepage (1440px)
- `Mobile.dc.html`: the mobile homepage (390px)
- `HeaderScroll.dc.html`: sticky-header behaviour and scroll animation (the logic and CSS are in the file)
